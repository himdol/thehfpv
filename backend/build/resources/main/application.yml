# Default Configuration
spring:
  profiles:
    active: local

# Server Configuration
server:
  port: 8080
  servlet:
    context-path: /api

# Application Configuration
app:
  name: TheHFPV Backend
  version: 1.0.0
  description: Backend API for TheHFPV project

# Logging Configuration
logging:
  level:
    root: INFO
    com.thehfpv: DEBUG
